# ChangeProof Evidence Pack — REPORT-GW Timeout Service

**Workload profile:** `timeout-service`
**Scenario:** A deliberately small modern Node.js workload used to prove that ChangeProof is not hard-coded to ORDERPRO or IBM i.

> Generated by the workload-neutral ChangeProof evidence core. Scenario-specific behavior is supplied through a profile and inference plug-in, not hard-coded into the core.

## Change request

# Change Request: CHG-WEB-017

## Title
Increase outbound request timeout for Report Gateway

## Affected System
REPORT-GW — small Node.js HTTP service

## Description
Increase the Report Gateway application request timeout from 30 seconds to 60 seconds. Long-running report generation has begun exceeding the existing 30-second application timeout during peak periods.

The requested change is limited to the application request timeout. No API contract or response-shape changes are requested.

## Acceptance Criteria
1. The application request timeout is 60 seconds.
2. Existing request handling remains unchanged apart from the timeout value.
3. The timeout configuration is reviewable in source and covered by a local automated test.

## Keywords
Keywords: timeout, request, API, gateway, 30, 60, seconds

## Evidence summary

- Total findings: **9**
- Tests: **2 passing / 0 failing / 0 pending**
- evidenceBasis: OBSERVED_SOURCE=6, INFERRED=1, EXECUTED_LOCAL=2
- status: OPEN=7, RESOLVED=2
- validationTarget: LOCAL=9

## Primary review chain

| Artifact | Symbol | Evidence basis | Status | Target | Finding |
|---|---|---|---|---|---|
| `config/edge-proxy.conf` | proxy(45) < app(60) | INFERRED | OPEN | LOCAL | The application request timeout is 60s, but the upstream proxy timeout remains 45s. Requests may be terminated by the proxy before the application timeout can take effect. Remediation: set the upstream timeout above 60s or document an intentional shorter boundary. |
| `src/service.js` | REQUEST_TIMEOUT_SECONDS | OBSERVED_SOURCE | OPEN | LOCAL | numeric literal 'REQUEST_TIMEOUT_SECONDS' = 60 — keywords: 60 |
| `docs/runbook.md` | Application request timeout: 60 seconds. | OBSERVED_SOURCE | OPEN | LOCAL | list item 'Application request timeout: 60 seconds.' = - Application request timeout: 60 seconds. — keywords: timeout, request, 60, seconds |
| `tests/timeout.test.js` | CHG-WEB-017 timeout acceptance application request timeout is 60 seconds | EXECUTED_LOCAL | RESOLVED | LOCAL | PASSING test: CHG-WEB-017 timeout acceptance application request timeout is 60 seconds |
| `tests/timeout.test.js` | CHG-WEB-017 timeout acceptance existing gateway behavior remains unchanged apart from timeout | EXECUTED_LOCAL | RESOLVED | LOCAL | PASSING test: CHG-WEB-017 timeout acceptance existing gateway behavior remains unchanged apart from timeout |

## Diff summary

- Resolved: **3**
- Persisted: **5**
- Target validation required: **0**
- New findings: **2**